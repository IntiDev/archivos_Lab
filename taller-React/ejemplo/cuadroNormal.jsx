import React from 'react';
import {Panel} from 'react-bootstrap';

class cuadroNormal extends React.Component { //clases con CamelCaseSiempre (si no no funciona)
	render(){
		return(
			let objetoPropiedades = {
			background: this.props.colorFondo,
			}
		);
	}
}
